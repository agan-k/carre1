import styled from 'styled-components';
import {Text} from '../../../shared';

export const SourceWrapper = styled(Text)(
  {
    textAlign: 'right',
    fontWeight: '600',
    fontStyle: 'italic',
    fontSize: '14px',
  }
);
